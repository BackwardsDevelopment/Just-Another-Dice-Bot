version: "3.9"

services:
  discord-bot:
    build: .
    container_name: dice-bot
    restart: unless-stopped
    tty: true
    volumes:
      - /srv/dice-bot/data:/app/data/
    environment:
      - TOKEN=${TOKEN}